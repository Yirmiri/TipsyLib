## Version Release v4.3.7 MC-1.20.1

### Additions
- Added `RLPlatformHelper.configDir()`, gets the config directory for the current loader

### Fixes
- `RLRegistryHelper.registerParticle` is now `SimpleParticleType` rather than `ParticleType<?>`
- Fixed a crash with smeltables